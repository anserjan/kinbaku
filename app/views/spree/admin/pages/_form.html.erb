<div data-hook='admin_page_form_fields'>
  <div class='row'>
    <div class='col-12'>
      <%= f.field_container :title do %>
        <%= f.label :title, class: 'required' %>
        <%= f.text_field :title, required: true, class: 'fullwidth title' %>
        <%= f.error_message_on :title %>
      <% end %>
    </div>

    <div class='col-12'>
      <%= f.field_container :slug do %>
        <%= f.label :slug, class: 'required' %>
        <%= f.text_field :slug, required: true, class: 'fullwidth title', disabled: f.object.new_record? %>
        <%= f.error_message_on :slug %>
      <% end %>
    </div>

    <div class="col-12">
      <%= f.field_container :menu_bar_id do %>
        <%= f.label :menu_bar_id %>
        <%= f.select :menu_bar_id, Spree::MenuBar.all.map { |mb| [mb.name, mb.id]}, { include_blank: t('spree.match_choices.none') }, { class: 'custom-select' } %>
      <% end %>
    </div>

    <div class='col-12'>
      <%= f.field_container :body do %>
        <%= f.label :body, class: 'required' %>
        <%= f.cktext_area :body, class: 'fullwidth', rows: 12 %>
        <%= f.error_message_on :body %>
      <% end %>
    </div>
  </div>

</div>