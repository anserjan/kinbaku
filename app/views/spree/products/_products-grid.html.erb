<%
  base_class = "products-grid".freeze

  # Optional props
  classes = local_assigns.fetch(:classes, [])

  # Classes
  class_names = classes.push(base_class).join(" ")
%>

<ul class="<%= class_names %>">
  <% products.sort_by{ |product| product.date_and_time }.each do |product| %>
    <%= render 'spree/products/product-card', product: product %>
  <% end %>
</ul>
